import { Stack, Typography } from '@mui/material';

const N10406 = () => {
  return (
    <Stack>
      <Typography variant='h1' marginTop={30} marginLeft={30}>
        박지후
      </Typography>
    </Stack>
  );
};

export default N10406;
